version: "3.8"

services:
  

  backend:
    build: ./backend
    container_name: backend
    ports:
      - "5000:5000"
    environment:
      - MONGODB_URI=mongodb+srv://evelyneotieno:HrwEb_Rfh-C82wu@week3.jcvhbbq.mongodb.net/?retryWrites=true&w=majority&appName=week3
    networks:
      - yolo-net

  frontend:
    build: ./client
    container_name: frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - yolo-net



networks:
  yolo-net:
    driver: bridge